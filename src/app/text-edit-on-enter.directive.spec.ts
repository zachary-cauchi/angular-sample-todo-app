import { TextEditOnEnterDirective } from './text-edit-on-enter.directive';

describe('TextEditOnEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new TextEditOnEnterDirective();
    expect(directive).toBeTruthy();
  });
});
